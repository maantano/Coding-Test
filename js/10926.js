const inputData = require("fs").readFileSync("/dev/stdin").toString().trim();
consle.log(`${inputData}??!`);

const input = require("fs").readFileSync("/dev/stdin").toString().trim();
console.log(`${input}??!`);
